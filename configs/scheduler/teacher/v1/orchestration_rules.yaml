{
  "version": "v1",
  "rules": [
    "Choose exactly one token from VALID_ACTIONS.",
    "Do not call or name tools; tools are controlled by the selected Expert Agent.",
    "Do not produce a trade decision or a complete future route.",
    "Research Manager requires non-empty research debate history.",
    "Trader requires a non-empty investment plan.",
    "Portfolio Manager requires a trader plan and non-empty risk history.",
    "STOP requires a non-empty final trade decision.",
    "Avoid repeating an Agent when the previous call produced no new state.",
    "Respect the remaining step and cost budget."
  ]
}
